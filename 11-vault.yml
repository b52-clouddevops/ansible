- name: Demo on vault  
  hosts: localhost              # Means telling to execute the playbook tasks on the ansible-controller itself 
  vars:
    PASS: abc123121
  tasks:  
    - name: Printing 
      debug: 
        msg: PASSWORD = {{PASS}}